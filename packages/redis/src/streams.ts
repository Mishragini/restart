/** Engine → archiver durable mutation log. */
export const ENGINE_ARCHIVER_STREAM = "engine:archiver"

/** Consumer group that persists mutations to Postgres. */
export const ARCHIVER_GROUP = "archiver"
